# array_average.rb
# write a method tat takes one argument, an array of integers
# and returns the average of all nums in the arr 

def average(arr)
  avg = arr.sum/arr.length.to_f #added the .to_f method based on the 'further exploration' section
end
